export function SponsorSlot() {
  return (
    <aside className="rounded-3xl border border-dashed border-white/15 bg-white/[0.02] px-5 py-5 sm:px-6">
      <p className="font-mono text-[11px] tracking-[0.22em] text-muted uppercase">Your ad here</p>
      <p className="mt-2 max-w-2xl text-sm leading-relaxed text-muted">
        Sponsorship slot for a later version. Nothing is sold, tracked, or injected on this page.
        The checker stays free.
      </p>
    </aside>
  );
}
